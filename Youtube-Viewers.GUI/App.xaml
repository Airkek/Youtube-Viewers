<Application x:Class="Youtube_Viewers.GUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Youtube_Viewers.GUI"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="flatButon" TargetType="{x:Type Button}">
             <Setter Property="Background" Value="Chocolate"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="HorizontalAlignment" Value="Center"/>
             <Setter Property="VerticalAlignment" Value="Center"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Padding" Value="10 5"/>
        </Style>
        
        <Style x:Key="beautyButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Chocolate"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="50 8"/>
            
        </Style>
    </Application.Resources>
</Application>
